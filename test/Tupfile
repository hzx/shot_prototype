include_rules

TEST_LIBS = -lgmock -lgtest -lpthread -lshot
RPATH = -Wl,-rpath="$"ORIGIN/../shot:"$"ORIGIN

: foreach *.cpp |> !cc |> %B.o {objs}
: {objs} | ../shot/libshot.so |> $(CC)  $(LFLAGS) -L../shot $(RPATH) -o %o %f $(TEST_LIBS) |> test
